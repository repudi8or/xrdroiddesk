<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Fully transparent, non-dimming theme for GlassesWakeActivity.
         The activity window is invisible but its presence wakes the display's
         WindowManager token infrastructure. -->
    <style name="Theme.GlassesWake" parent="android:Theme">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>
